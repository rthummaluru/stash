{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the overlay window",
  "windows": ["test_overlay"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
